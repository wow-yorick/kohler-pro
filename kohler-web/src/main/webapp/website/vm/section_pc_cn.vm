<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8"/>
    <meta name='keywords' content='$!{data.seo.get(0).seo_keywords}'/>
    <meta name='description' content='$!{data.seo.get(0).seo_description}'/>
    #parse("style.vm")
    <title>$!{data.seo.get(0).seo_title}</title>
	  
</head>
<body>
<div class="head_wrapper">
#parse("header_pc_cn.vm")
   
  <div class="pub_submenu page_menu clearfix">
		
	
        <div class="submenu_panel actived">
            <ul>
			#if(${data.Categories})
			#foreach($Categories in ${data.Categories})
				
				<li>
                    <p index="kitchen_list$velocityCount">$!{Categories.category_name}</p>
				</li>
				
				#set($heart=$velocityCount)
				#end
				#end
				#if(${data.suites})
				#set($heart=$heart+1)
				<li>
                    <p index="kitchen_list$heart">套间</p>
				</li>
				#end
				#if(${data.suites})
				#set($heart=$heart+1)
				<li>
                    <p index="kitchen_list$heart">卫浴新品</p>
				</li>
				#end
            </ul>
        </div>
		
    </div>
    
</div>  
<div class="content_wrapper">

<div class="pub_banner product">
    <div class="subwrapper kitchen">
	#if(${data.hero_areas})
	 #foreach($hero_areas in ${data.hero_areas})
	 
	 
	 #if($velocityCount==1)
	 #if(${hero_areas.type}==10080001)
	 <div class="bgimg actived"><a href="${hero_areas.target_url}" target="${hero_areas.operation}"><img src="${hero_areas.image_url}" alt="${hero_areas.image_alt}"	style="width:1280px;height:500px;"></a></div>
	 #end
	 
	  #if(${hero_areas.type}==10080002)<!--type为10080002 的是视频-->
		    <div class="bgimg actived"><div id="dd$velocityCount" class="d0"></div>
			 <script type='text/javascript'>
			$(function(){
				/**
				 * 使用说明
				 * 首先载入上方的 CSS文件以及JS文件
				 * 本播放器强制了FLASH格式，也就是说只支持FLV视频格式，如果要兼容其他视频格式，需要调整JS代码
				 * SWF为FLASH播放器的路径地址
				 * 参数说明
				 * arguments[0]:需要载入播放器的容器ID
				 * width : 播放器宽度
				 * height : 播放器的高度
				 * autoplay : 是否自动播放
				 * loop : 是否循环播放
				 * poster ： 播放器播放之前展示的图片
				 * flv : Flash 视频的地址
				 */
				// 视频播放器代码
				var myVideo = new MYPUMA.PumaVideo('$!{data.base_url}js/video-js.swf');
				myVideo.create('dd$velocityCount',{'width':1280,'height':500,'autoplay':false,'loop':false,'poster':'${hero_areas.image_url}','flv':'${hero_areas.video_url}'});
				var videoobj = _V_($('#dd$velocityCount').attr('videoid'));
				videoobj.addEvent('pause',function(){
					console.log(this.el);
					$(this.el).find('img.vjs-poster').css('display','block');

				});
			});
        
			</script>
			</div>
			
		#end
		#end
		
		
		 #if($velocityCount!=1)
	 #if(${hero_areas.type}==10080001)
	 <div class="bgimg"><a href="${hero_areas.target_url}" target="${hero_areas.operation}"><img src="${hero_areas.image_url}" alt="${hero_areas.image_alt}"	style="width:1280px;height:500px;"></a></div>
	 #end
	 
	  #if(${hero_areas.type}==10080002)<!--type为10080002 的是视频-->
		    <div class="bgimg"><div id="dd$velocityCount" class="d0"></div>
			 <script type='text/javascript'>
			$(function(){
				/**
				 * 使用说明
				 * 首先载入上方的 CSS文件以及JS文件
				 * 本播放器强制了FLASH格式，也就是说只支持FLV视频格式，如果要兼容其他视频格式，需要调整JS代码
				 * SWF为FLASH播放器的路径地址
				 * 参数说明
				 * arguments[0]:需要载入播放器的容器ID
				 * width : 播放器宽度
				 * height : 播放器的高度
				 * autoplay : 是否自动播放
				 * loop : 是否循环播放
				 * poster ： 播放器播放之前展示的图片
				 * flv : Flash 视频的地址
				 */
				// 视频播放器代码
				var myVideo = new MYPUMA.PumaVideo('$!{data.base_url}js/video-js.swf');
				myVideo.create('dd$velocityCount',{'width':1280,'height':500,'autoplay':false,'loop':false,'poster':'${hero_areas.image_url}','flv':'${hero_areas.video_url}'});
				var videoobj = _V_($('#dd$velocityCount').attr('videoid'));
				videoobj.addEvent('pause',function(){
					console.log(this.el);
					$(this.el).find('img.vjs-poster').css('display','block');

				});
			});
        
			</script>
			</div>
			
		#end
		#end
		#end
		
		#if(${data.hero_areas})
		 <div class="logo_bar">
			
			#foreach($hero_areas in ${data.hero_areas})
			#if(${hero_areas.type}==10080001)<!--type为10080001 的是图片-->
				$!{hero_areas.image_overlay_text}
			#end
		 
			#if(${hero_areas.type}==10080002)<!--type为10080002 的是视频-->
				<a href="" class="logo">
                <h2><i class="mainlogo"></i></h2>
    
                <p></p>
				</a>
			#end
			
			#end
			
		 
		 </div>
		 #end
		
        <div class="banner_page product_icon clearfix">
		 #foreach($hero_areas in ${data.hero_areas})
		 #if(${velocityCount}==1)
			 <span index="${velocityCount}" class="selected"></span>
			#else
			 <span index="${velocityCount}"></span>
			#end
		#end
		#end
        </div>
    </div>
</div>
<div class="product_content">
	#if(${data.Categories})
	
	#foreach($Categories in ${data.Categories})
	
    <div class="list" id="kitchen_list$velocityCount">
        <div class="column clearfix">
            <div class="title clearfix">
                <h2 class="left">$!{Categories.category_name}</h2>
                <a class="right" href="$!{Categories.url}">查看所有$!{Categories.category_name}产品<img class="arrow" style="float:right" src="$!{data.base_url}images/arrow1.png"
                                                        alt=""></a>
            </div>
			#set($aa=$velocityCount)
			 #foreach($product in ${Categories.banner_units})
			 #if(${product.type}==10080001)<!--type为10080001的是图片-->
            <div class="item"><a href="$!{product.target_url}"  >
                <div class="item_img">
                    <img src="$!{product.file_id}" alt="$!{product.image_alt}" 	style="width:300px;height:230px;"/>
                </div>
					$!{product.image_overlay_text}	</a>	 
            </div>
			#end
			#if(${product.type}==10080002)<!--type为10080002的是视频-->
			<div class="item">
				<div class="item_img"><!-- <img src="images/bathroom_item1.jpg" alt=""/> --><div id='d${aa}_$velocityCount'></div></div>
							$!{product.image_overlay_text}
			</div>
			<script type='text/javascript'>
				$(function(){
					/**
					 * 使用说明
					 * 首先载入上方的 CSS文件以及JS文件
					 * 本播放器强制了FLASH格式，也就是说只支持FLV视频格式，如果要兼容其他视频格式，需要调整JS代码
					 * SWF为FLASH播放器的路径地址
					 * 参数说明
					 * arguments[0]:需要载入播放器的容器ID
					 * width : 播放器宽度
					 * height : 播放器的高度
					 * autoplay : 是否自动播放
					 * loop : 是否循环播放
					 * poster ： 播放器播放之前展示的图片
					 * flv : Flash 视频的地址
					 */
					// 视频播放器代码

							var myVideo = new MYPUMA.PumaVideo('$!{data.base_url}js/video-js.swf');
							myVideo.create('d${aa}_$velocityCount',{'width':312,'height':220,'autoplay':false,'loop':false,'poster':'$!{product.file_id}','flv':'$!{product.video_url}'});
				});
        
			</script>
			#end
			
			#end
           
        </div>
    </div>

   #end
	
   #end
   
   
     <!--套间-->
   #if(${data.suites})
   #set($aa=$aa+1)
    <div class="list" id="kitchen_list$aa">
        <div class="column clearfix">
            <div class="title clearfix">
                <h2 class="left">套间</h2>
                <a class="right" href="">查看所有套间产品<img class="arrow" style="float:right" src="$!{data.base_url}images/arrow1.png"
                                                        alt=""></a>
            </div>
			
			 #foreach($suites in ${data.suites})
			 #if(${suites.type}==10080001)<!--type为10080001的是图片-->
            <div class="item"><a href="$!{suites.target_url}"  >
                <div class="item_img">
                    <img src="$!{suites.file_id}" alt="$!{suites.image_alt}" 	style="width:300px;height:230px;"/>
                </div>
					$!{suites.image_overlay_text}	</a>	 
            </div>
			#end
			#if(${suites.type}==10080002)<!--type为10080002的是视频-->
			<div class="item">
				<div class="item_img"><!-- <img src="images/bathroom_item1.jpg" alt=""/> --><div id='dNew$velocityCount'></div></div>
							$!{suites.image_overlay_text}
			</div>
			<script type='text/javascript'>
				$(function(){
					/**
					 * 使用说明
					 * 首先载入上方的 CSS文件以及JS文件
					 * 本播放器强制了FLASH格式，也就是说只支持FLV视频格式，如果要兼容其他视频格式，需要调整JS代码
					 * SWF为FLASH播放器的路径地址
					 * 参数说明
					 * arguments[0]:需要载入播放器的容器ID
					 * width : 播放器宽度
					 * height : 播放器的高度
					 * autoplay : 是否自动播放
					 * loop : 是否循环播放
					 * poster ： 播放器播放之前展示的图片
					 * flv : Flash 视频的地址
					 */
					// 视频播放器代码

							var myVideo = new MYPUMA.PumaVideo('$!{data.base_url}js/video-js.swf');
							myVideo.create('dNew$velocityCount',{'width':312,'height':220,'autoplay':false,'loop':false,'poster':'$!{suites.file_id}','flv':'$!{suites.video_url}'});
				});
        
			</script>
			#end
			
			#end
           
        </div>
    </div>
	#end
   
   
   
   <!--新品-->
   #if(${data.new_arrival})
   #set($aa=$aa+1)
    <div class="list" id="kitchen_list$aa">
        <div class="column clearfix">
            <div class="title clearfix">
                <h2 class="left">卫浴新品</h2>
                <a class="right" href="">查看所有卫浴新品<img class="arrow" style="float:right" src="$!{data.base_url}images/arrow1.png"
                                                        alt=""></a>
            </div>
			
			 #foreach($new_arrival in ${data.new_arrival})
			 #if(${new_arrival.type}==10080001)<!--type为10080001的是图片-->
            <div class="item"><a href="$!{new_arrival.target_url}"  >
                <div class="item_img">
                    <img src="$!{new_arrival.file_id}" alt="$!{new_arrival.image_alt}" 	style="width:300px;height:230px;"/>
                </div>
					$!{new_arrival.image_overlay_text}		</a> 
            </div>
			#end
			#if(${new_arrival.type}==10080002)<!--type为10080002的是视频-->
			<div class="item">
				<div class="item_img"><!-- <img src="images/bathroom_item1.jpg" alt=""/> --><div id='dNew$velocityCount'></div></div>
							$!{new_arrival.image_overlay_text}
			</div>
			<script type='text/javascript'>
				$(function(){
					/**
					 * 使用说明
					 * 首先载入上方的 CSS文件以及JS文件
					 * 本播放器强制了FLASH格式，也就是说只支持FLV视频格式，如果要兼容其他视频格式，需要调整JS代码
					 * SWF为FLASH播放器的路径地址
					 * 参数说明
					 * arguments[0]:需要载入播放器的容器ID
					 * width : 播放器宽度
					 * height : 播放器的高度
					 * autoplay : 是否自动播放
					 * loop : 是否循环播放
					 * poster ： 播放器播放之前展示的图片
					 * flv : Flash 视频的地址
					 */
					// 视频播放器代码

							var myVideo = new MYPUMA.PumaVideo('$!{data.base_url}js/video-js.swf');
							myVideo.create('dNew$velocityCount',{'width':312,'height':220,'autoplay':false,'loop':false,'poster':'$!{new_arrival.file_id}','flv':'$!{new_arrival.video_url}'});
				});
        
			</script>
			#end
			
			#end
           
        </div>
    </div>
	#end
   
  
   
</div>
<div class="pub_totop">
    <div class="totop_btn"><img src="$!{data.base_url}images/totop.png" alt=""></div>
    <span>返回顶部</span>
</div>
#parse("footer_pc_cn.vm")
</body>
</html>