package com.kohler.constants;

public class ConfigurationSQL {

        public static final String UPDATE_CONFIGURACTION = "UPDATE MASTERDATA SET MASTERDATA_NAME = ? "
                + "WHERE MASTERDATA_METADATA_ID = ?";
}
